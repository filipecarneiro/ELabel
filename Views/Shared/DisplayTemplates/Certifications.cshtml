@model Certifications

<div class="text-center">
    <div class="row align-items-start">
        @if(Model.Organic)
        {
        <div class="col">
            <img
                class="img-fluid m-3"
                style="max-height: 75px"
                loading="lazy"
                src="~/img/certification-organic.svg"
                alt="@ViewData.ModelMetadata.Properties.Where(m => m.PropertyName == "Organic").FirstOrDefault()?.Description"
                title="@ViewData.ModelMetadata.Properties.Where(m => m.PropertyName == "Organic").FirstOrDefault()?.Description"
            />
        </div>
        }
    </div>
</div>